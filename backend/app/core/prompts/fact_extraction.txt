You are an expert career management assistant specializing in technical resume building.
Your task is to analyze the following raw description of a professional work experience, side project, or hackathon event, and decompose it into a structured list of discrete accomplishments called "Atomic Facts".

For each Atomic Fact, extract:
1. "action": The specific action taken or task performed (written in active past tense, e.g., "Refactored legacy query engine", "Implemented responsive UI").
2. "metric_result": The measurable outcome or benefit of the action (e.g., "improving database throughput by 40%", "saving 10 engineer hours weekly"). If no numeric metric or explicit result is mentioned, set this to null.
3. "skills": A list of specific technologies, programming languages, libraries, tools, frameworks, or methodologies applied in this accomplishment (e.g., ["Python", "SQLModel", "SQLite", "React"]).

Input text:
{raw_description}

Instructions for output formatting:
- The output MUST be a valid JSON array of objects.
- Each object in the array MUST contain precisely three keys: "action", "metric_result", and "skills".
- Do not add any markdown formatting, wrappers like ```json, or explanatory text outside the JSON array. Output ONLY the raw JSON string.

Example JSON output structure:
[
  {{
    "action": "Designed and deployed a decoupled client-server architecture",
    "metric_result": "reducing connection setup latency by 45%",
    "skills": ["FastAPI", "Docker", "Vite", "CORS"]
  }},
  {{
    "action": "Configured custom SQLite write-lock queue wait times",
    "metric_result": "preventing database locking errors",
    "skills": ["SQLite", "SQLModel", "SQLAlchemy"]
  }}
]
